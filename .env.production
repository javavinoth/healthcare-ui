# Healthcare Application - Production Environment Configuration

# API Configuration
# NOTE: Backend uses /api as servlet context path (server.servlet.context-path: /api)
# Using relative URL for production deployment (frontend and backend on same domain)
# If deploying to different domains, use full URL: https://api.yourdomain.com/api
VITE_API_URL=/api

# Application Info
VITE_APP_NAME=Healthcare Portal
VITE_APP_VERSION=1.0.0

# Environment
VITE_ENVIRONMENT=production

# Security Settings
# Session timeout in milliseconds (15 minutes for production)
VITE_SESSION_TIMEOUT=900000

# Feature Flags
VITE_ENABLE_2FA=true
VITE_ENABLE_BIOMETRIC_AUTH=false
VITE_ENABLE_TELEHEALTH=true

# Analytics (HIPAA-compliant only!)
# Do NOT enable without proper HIPAA-compliant analytics service and BAA
VITE_ENABLE_ANALYTICS=false

# Development Tools (DISABLED in production)
VITE_ENABLE_REACT_QUERY_DEVTOOLS=false
VITE_ENABLE_DEBUG_LOGS=false
